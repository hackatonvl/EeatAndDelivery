<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>
<h2>Корзина</h2>
<ul>
	<% @cart.line_items.each do |item| %>
		<li><%= item.quantity %> &times; <%= item.product.title %> - <%= number_to_currency(item.total_price) %> - <%= link_to 'x', item, :method => :delete %></li>
	<% end %>
</ul>
Итого: <%= number_to_currency(@cart.total_price) %>

<%= link_to 'Вернуться к покупкам', store_path %>

<%= button_to "Checkout", new_order_path, method: :get %>